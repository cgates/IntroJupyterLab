name: jupyterlab
channels:
  - conda-forge
  - defaults
dependencies:
  - jupyterlab
  - nodejs
  - matplotlib
  - scipy # only strictly necessary for the demo notebook
  - pandas # only strictly necessary for the demo notebook
  - seaborn # only strictly necessary for the demo notebook
  - pytest # only for testing
