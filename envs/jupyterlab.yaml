name: jupyterlab
channels:
  - conda-forge
  - defaults
dependencies:
  - jupyterlab
  - nodejs
  - matplotlib
  - ipywidgets # interactive UI components *
  - scipy # for lorenz interactive demo *
  - pandas # for data frame operations *
  - seaborn # plotting library *
  - pytest # for automated testing

# * only strictly necessary for the demo notebook
